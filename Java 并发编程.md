# Java 并发编程

## 1、说说进程和线程的区别

### 何为进程？

程序由指令和数据组成，但这些指令要运行，数据要读写，就必须将指令加载至 CPU，数据加载至内存。在指令运行过程中还需要用到磁盘、网络等设备。进程就是用来加载指令，管理内存，管理 IO 的。

当一个程序被运行，从磁盘加载这个程序的代码至内存，这时就开启了一个进程。

**进程就可以视为程序的一个实例**，大部分程序可以同时运行多个实例进程（例如，记事本、画图、浏览器等），也有的程序只能启动一个实例进程（如网易云音乐、360卫士等）。

背：【

**进程是程序的一次执行过程**，**是操作系统进行资源分配、运行程序的基本单位**，因此进程是**动态**的，

在 Java 中，当我们运行 main 函数时，其实就是启动了一个 JVM 的进程，而 main 函数所在的线程就是这个进程中的一个线程，也称主线程。】

### 何为线程？

一个进程之内可以分为一到多个线程

一个线程就是一个指令流，将指令流中的一条条指令以一定的顺序交给 CPU 执行。

Java 中，线程作为最小调度单位，进程作为资源分配的最小单位。 

背：【

线程是进程中的独立执行单元，是操系统的最小调度单位，多个线程可以共享同一个进程的**堆**和**方法区**资源，但每个线程有自己的**虚拟机栈**、**栈帧**、**程序计数器**，所以系统在产生一个线程，或是在各个线程之间做切换工作时，**负担要比进程小得多**。】

![image-20260118183002602](../Spring大礼包学习/img/image-20260118183002602.png)

### 进程间是如何进行通信的？

原则上可以通过消息传递和共享内存两种方式来实现。Java 采用的是**共享内存的并发模型**。

这个模型被称为 Java 内存模型，简称 JMM，它决定了一个线程对共享变量的写入，何时对另外一个线程可见。当然了，本地内存是 JMM 的一个抽象概念，并不真实存在。

## 5、线程有哪些常用的调度方法

### 说说 sleep 方法

当线程 A 调用了 Thread 的 sleep 方法后，线程 A 从 Running  进入 Timed Waiting 状态（阻塞），线程 A 会暂时让出指定时间的执行权。指定的睡眠时间到了后该方法会正常返回，接着参与 CPU 调度，获取到 CPU 执行权后可以继续执行。

### 说说yield方法？

调用 yield 会让当前线程让出 CPU 执行权，从 Running 进入 Runnable  就绪状态，具体的实现依赖于操作系统的任务调度器。

### 说说interrupt方法？

在 Java 多线程的世界里，`interrupt()` 方法经常被误解。很多人以为它是“强制杀掉线程”，但实际上它更像是一个**“温和的敲门声”**。

**背**：

【

**它绝对不会直接终止正在运行的线程！** 如果线程正在算圆周率，它根本不会理会这个标志，除非你自己在代码里去检查它。	

`interrupt()` 方法用于通知线程**停止**，但不会直接终止线程，需要线程自行处理中断标志。打断 sleep、wait、join 等处于阻塞状态的 的线程，JVM 会强行把它立即唤醒，并抛出一个 InterruptedException 异常，会清空打断状态。打断正常运行的线程不会清空打断状态。常与 `isInterrupted()` 或 `Thread.interrupted()` 配合使用。

】

执行 `interrupt()` 时，底层通常会隐式地插入写屏障。这会强制将当前线程工作内存中的缓存刷新到主内存中。

当其他线程通过 `isInterrupted()` 检查状态时，它会触发读屏障，强制从主内存读取最新状态，同时也使得之前 HB 关系链中的变量写入变得可见。

### 如何优雅的停止线程？

## 7、什么是线程上下文切换

线程上下文切换，是操作系统为了让多个线程轮流使用 CPU 而进行的一种“状态保存与回复”的过程。

## 8、守护线程了解吗

默认情况下，Java 进程需要等待所有线程都运行结束，才会结束。有一种特殊的线程叫做守护线程，只要其他非守护线程运行结束了，即使守护线程的代码没有执行完，也会强制结束。它的作用就是为其他线程服务，像 Java 中的**垃圾回收线**程就是一个守护线程。

### 守护线程和用户线程有什么区别呢？

Java 进程需要等待所有非守护进程都运行结束，才会结束。只要其他非守护线程运行结束了，即使守护线程的代码没有执行完，也会强制结束。

## 21、i++是原子操作吗？

不是，对于存在于堆中的成员变量和静态变量，从字节码的层面来看 i++ 对应 4 条指令，

第一条指令用于获取变量 i 的值，第二条指令用于准备常量 1，第三条指令用于自增，第四条指令用于将修改后的值写回。

如果反编译一条对于局部变量的自增自减操作，会发现其变成了一条指令，**但是这也不意味着它就是原子的**，因为局部变量本身就是线程私有的，存在于每个线程的栈帧中，**它根本不存在多线程竞争的问题**。只有存在于**堆（Heap）**中的实例变量或静态变量才需要考虑原子性。

## 25.🌟volatile 了解吗？

### volatile 如何保证变量的可见性

volatile 关键字，它可以用来修饰成员变量和静态成员变量，它**可以避免线程从自己的工作缓存中查找变量的值**。如果我们将变量声明为 volatile，这就指示 JVM 这个变量是共享且不稳定的，**必须到主存中获取它的值**，**线程操作 volatile 变量都是直接操作主存**。

然后我说一下它的底层原理：

volatile 的底层实现原理是**内存屏障**，Memory Barrier。对 volatile 变量的写指令后会加入写屏障，对 volatile 变量的读指令前会加入读屏障。

写屏障（sfence）保证在该屏障之前，对共享变量的改动，都会同步到主存当中。

而读屏障（Ifence）保障在该屏障之后，对共享变量的读取，加载的是主存中最新的数据。

**🌟这个八股待定，和面渣的回答有一点点出入，待真正了解完 JMM 后再完善。**

### volatile 可以保证原子性吗？

**`volatile` 关键字能保证共享变量的可见性，但不能保证原子性。**`synchronized` 关键字两者都能保证。但缺点是 synchronized 是属于重量级操作，性能相对更低。

## 26、synchronized 用过吗？

## 27、synchronized 的底层原理了解吗？

synchronized 依赖 JVM 中的 Moniter 实现线程同步，synchronized 加锁代码块（临界区）时，通过 **`monitorenter`**和**`monitorexit`**两个指令来实现的，其中 moniterenter 代表获取锁的开始，moniterexit 代表释放锁。

当线程执行到`monitorenter` 指令时，其通过锁对象的 Mark Word 中存储的 Moniter 的地址找到 Moniter，看其中的 Owner 是否为空，如果为空，将 Owner 指向自己。如果 Moniter 已被占用，线程进入 EnterList 阻塞等待。

字节码中包含一个 `monitorenter` 指令以及两个 `monitorexit` 指令，这是为了保证锁在同步代码块代码正常执行以及出现异常的这两种情况下都能被正确释放。

为了防止死锁，JVM 会确保无论同步代码块的代码是正常结束还是抛出异常，都会执行 moniterrexit。

![image-20260118230516389](../Spring大礼包学习/img/image-20260118230516389.png)

## 30、⭐️synchronized 和 ReentrantLock 有什么区别？

`synchronized` 是依赖于 JVM 层面实现的，`ReentrantLock` 是 JDK 层面实现的。（这句话摘自 JavaGuide，这里写这句话的目的是，等学完 ReentrantLock 的底层原理后，考虑一下这里要不要用 JavaGuide 的这句话所在的段落？还是就用下面这句面渣里的话呀。）

synchronized 由 JVM 内部的 Moniter 机制实现，ReentrantLock 基于 AQS 实现。

synchronized 不管临界区中的代码是否有异常，都能在字节码层面实现解锁。ReentrantLock 需要 lock() unlock() 方法配合 try-finally 语句块来完成。

相比`synchronized`，`ReentrantLock`增加了一些高级功能。主要来说有三点：

**可打断**：`ReentrantLock`提供了一种能够中断线程等待锁的机制，通过 `lock.lockInterruptibly()` 来实现。也就是说，如果线程1在等待获取锁的过程中，如果其他线程调用了线程1的 interrupt() 方法，可以中断线程1的等待过程，线程 1 就会抛出`InterruptedException` 异常，可以捕获该异常进行相应处理。

**可以设置为公平锁**：synchronized 只能是非公平锁，ReentrantLock 默认是非公平的，但可以设置为公平锁，只需要在创建 ReentrantLock 对象的时候，调用含一个参数的构造方法，传递参数 true 就可以了。所谓公平锁，就是大家都在排队等待锁，先到先得，不是说靠争抢去争取锁。

**可以设置超时时间**：`ReentrantLock` 提供了 `tryLock(timeout)` 的方法，可以指定等待获取锁的最长等待时间，如果超过了等待时间，就会获取锁失败，不会一直等待。

**通知机制更强大**：ReentrantLock 通过绑定多个 Condition 对象，可以实现分组唤醒和选择性通知，这解决了 synchronized 只能随机唤醒或全部唤醒的问题，为复杂的线程协作场景提供了强大的支持。

## 42.🌟聊聊悲观锁和乐观锁？（补充）

乐观锁和悲观锁不是具体的“锁实现”，是处理并发冲突的两种不同的思想。

### 什么是悲观锁

悲观锁认为竞争总是存在的，认为每次去访问共享资源的时候都会发生冲突，所以在操作前都会先加锁，防止其他线程修改数据。这样就能保证，共享资源每次只能给一个线程使用，其他线程阻塞。

像 Java 中`synchronized`和`ReentrantLock`等独占锁就是悲观锁思想的实现。

**优点是**：适合写操作密集的场景，保证数据强一致性。

**缺点是**：频繁的加锁、解锁，以及线程切换非常耗费 CPU。容易引发死锁问题。

### 什么是乐观锁

背：

【

乐观锁认为访问共享资源不会总是发生冲突，不怕别的线程来修改共享资源，就算修改了也没关系，吃亏点儿再重试呗。所以在操作前不加锁，只是在提交修改的时候去验证对应的资源是否被其他线程修改了。如果发现对应的共享资源被修改了，就会重试。

在 Java 中`java.util.concurrent.atomic`包下面的原子变量类（比如`AtomicInteger`、`LongAdder`）就是使用了乐观锁的一种实现方式 **CAS** 实现的。

**优点**：高并发场景下，读比较多、冲突较少的情况下，乐观锁相比悲观锁来说，不存在锁竞争造成线程阻塞，也不会有死锁问题，在性能上往往会更胜一筹。

**缺点**：但是，如果冲突频繁发生（写占比非常多的情况），会频繁失败并重试，这样同样会非常影响性能，白白浪费 CPU。

不过，大量失败重试的问题也是可以解决的，像我们前面提到的 `LongAdder`以空间换时间的方式就解决了这个问题。

理论上来说：

悲观锁通常用于写比较多、竞争激烈的情况，这样可以避免频繁失败重试影响性能，因为频繁失败重试会导致 CPU 占用率 100% 但啥事没干成。此时悲观锁通过“阻塞”让线程睡觉，反而保护了 CPU。不过，如果乐观锁解决了频繁失败和重试这个问题的话，比如`LongAdder`，也是可以考虑使用乐观锁的，要视实际情况而定。

乐观锁通常用于读比较多、冲突较少的情况，这样可以避免频繁加锁影响性能。不过，乐观锁主要针对的对象是单个共享变量（参考`java.util.concurrent.atomic`包下面的原子变量类）。

】

**低/中度竞争**：吞吐量冠军：乐观锁。原因：几乎不失败，没有锁开销，CPU 全速处理业务。

**极高度竞争**：吞吐量冠军：**悲观锁**。原因：乐观锁会陷入无休止的“失败-重试”循环（自旋），导致 CPU 占用率 100% 但啥事没干成。此时悲观锁通过“阻塞”让线程睡觉，反而保护了 CPU。

CAS 和 volatile 结合实现无锁并发，必须是在多核心 CPU 下才能发挥优势，在单核 CPU 下如果没有分到时间片的话，还是会发生上下文切换，效率可能也不会比 synchronized 高.     就 CAS 和 volatile 结合实现的乐观锁

## 什么是 Moniter？

从广义上讲，Moniter 是一种**同步工具**，用于保证在同一时刻，只有一个线程能够执行特定的代码段（**临界区**）。

在 Monitor 的语境下，**“同步”= 互斥 + 协作**。

- 它通过**锁**实现了“互斥”。
- 它通过**等待/通知机制**实现了“协作”。

它最早由计算机科学家 Hoare 和 Brinch Hansen 提出。它的核心思想是：将共享变量和对这些变量的操作封装在一起，并提供一种机制，确保线程互斥的访问这些操作。

### Java 中的 Moniter 是什么？

**在 Java 中 Moniter 是 synchronized 关键字实现的底层基石**。Java 的设计者提出了一个口号：**“Everything is an Object”**，与之对用的是“**每个 Java 对象天生就是一把 Moniter 锁。**（又称内置锁或监视器锁）”

**在 JVM 源码中**，**Moniter 是由一个名为 ObiectMoniter 的 C++ 对象实现的**。它包含几个关键区域：_owner、_EntryList、WaitSet。

### Moniter 的字节码表现

如果你反编译 Java 代码，你会看到两条关键指令：

moniterenter：代表获取锁的开始。

moniterexie：代表释放锁。为了防止死锁，JVM 会确保无论代码是正常结束还是抛出异常，都会执行 moniterrexit。

### 总结：Moniter 的三层含义

为了不混淆，你可以根据语境这样理解：

1、同步机制层：它是一种保证线程安全的程序设计范式（管程）。

2、Java 语言层：它是 synchronized 的别名，即“监视器锁”。

3、JVM 实现层：它是堆中对象头中的 Mark Word 关联的一个 ObjectMoniter C++ 对象。

### 操作系统中有 Moniter 吗？

没有。通常被认为是一种**高级编程语言级别的**同步构造。它是由编译器或运行时环境（如 JVM）封装好的。

 操作系统提供最基本的同步工具：如：**Mutex（互斥锁）**、**Semaphore（信号量）** 和 **Condition Variables（条件变量）**。

Moniter 是**对这些底层工具的一种高级封装。**



Moniter 的两大作用：互斥、同步（此处的同步指线程间协作）。

## 同步（Synchronization）

在编程语境下，“同步”指的是：协调多个线程的执行步调，确保他们按照**预期的顺序**、**安全的**访问共享资源。

简单来说同步就是为了解决两个核心问题：“**别一起动**”（互斥）和“**按顺序动**”（协作）。

### 1、同步的第一层含义：互斥（Mutual Exclusion）

这是“同步”最常见的应用。用于保证在同一时刻，只有一个线程能够执行特定的代码段（**临界区**）。这就是“**同步访问**”

### 2、同步的第二层含义：协作

同步不仅仅是“**加锁**”，它还负责协调线程之间的先后顺序。有时候，线程 A 必须等待线程 B 完成某个任务后才能继续执行。这就是“**同步等待**”

### 为什么叫“**同步**”不叫“**异步**”

**异步（Asynchronous）：** 意味着“各忙各的”，互不干扰。你发个邮件，然后去喝咖啡，邮件什么时候发完你不管。

**同步（Synchronous）：** 意味着“相互制约”。多个线程必须在某些点上“对齐”步调——你要等我释放锁，我要等你的执行结果。这种**步调的一致性**，就是“同步”一词的来源。

## 用户态与内核态

### 用户态

简单来说，“用户态”（User Mode）是操作系统为例保护系统安全，给**应用程序**划定的一块“安全活动区”。**在用户态运行的代码**，**无法直接访问硬件**，也**无法访问其他程序的内存**。

用户态 vs. 内核态

| **特性**     | **用户态 (User Mode)**               | **内核态 (Kernel Mode)**         |
| ------------ | ------------------------------------ | -------------------------------- |
| **运行主体** | 你写的代码、浏览器、Java虚拟机       | 操作系统内核、驱动程序           |
| **权限级别** | 低（受限）                           | 最高                             |
| **硬件访问** | **禁止**直接访问（**必须求助内核**） | **允许**直接操作硬件             |
| **安全性**   | 高（即便崩溃，通常只倒一个软件）     | 风险大（一旦崩溃，整个系统死机） |

### 它们之间如何沟通？

如果用户态的程序想写一个文件（涉及硬盘），它该怎么办呢？它不能直接动硬盘，它必须发起一个“系统调用”。

1、**发出请求：** 程序在柜台外喊一声：“我要存钱（写文件）！”

2、**模式切换：** 此时发生一次**上下文切换**，**CPU 从用户态切换到内核态**。

3、**内核处理：** 银行职员（内核）接管，检查你的权限，然后代你操作硬盘。

4、**返回结果：** 操作完后，CPU 切回用户态，把结果告诉程序。

现代 JVM 会想尽办法在“用户态”就把锁的问题解决掉，比如偏向锁，轻量级锁。

## 偏向锁

轻量级锁在没有竞争时，（就自己这个线程），每次重入仍然需要执行 CAS 操作。

Java 6 中引入了偏向锁来做进一步优化：只有第一次使用 CAS 将线程 ID 设置到对象头中的 Mark Word 中，之后发现这个线程 ID 时自己的就表示没有竞争，不用重新 CAS。以后只要不发生竞争，这个对象就归该线程所有。

## 批量重偏向

当一个类的对象发生频繁的撤销时（默认达到 20 次），JVM 会认为：“看来这个类之前的偏向设置得不对，**我不应该一个个去撤销**，**而应该重新决定这些对象偏向谁。**”

JVM 不会去逐个扫描所有对象的栈，而是直接修改类结构中的一个 epoch 计数器。

下次线程访问这些对象时，只需检查对象的 epoch 和类的 epoch 是否一致，如果不一致，直接通过一个简单的 **CAS 操作** 把自己设为新主人。

## ReentrantLock 的基本语法中为什么要用 try-finally？不用不行吗？

这个结构之所以被称为“黄金准则”，是因为多线程编程中，**“锁的释放”比“锁的获取”更重要**。

1. **为什么要用 `try-finally`？**

使用 `finally` 块可以保证：**无论临界区代码是正常执行完毕，还是中途因异常崩溃，`unlock()` 都会被执行。**

2. **不用 `try-finally` 行不行？**

从语法上讲，**行**。编译器不会报错，程序也能跑。 但从工程实践上讲，**绝对不行**。

3. **一个非常关键的细节：`lock()` 放在哪里？**

为什么要放在外面？

- **防止误解锁：** 如果 `lock()` 放在 `try` 内部，万一 `lock()` 方法本身抛出了异常（虽然极少见，但在某些特殊 Lock 实现中可能发生），程序会直接进入 `finally` 执行 `unlock()`。

- **逻辑矛盾：** 此时线程其实**并没有成功持有锁**，去调用 `unlock()` 会抛出 `IllegalMonitorStateException`（正如你上一条提问中遇到的情况）。

## 在并发编程语境下“高吞吐量”的含义

“高吞吐量”主要指**单位时间内系统能够完成的业务操作数量**。通常用 TPS 或 QPS 来衡量。