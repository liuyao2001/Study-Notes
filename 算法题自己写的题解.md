# 算法题自己写的题解

# 一、力扣hot100

## 一、简单

### 1、两数之和

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值** *`target`* 的那 **两个** 整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。

你可以按任意顺序返回答案。

**示例 1：**

```
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> hashtable = new HashMap<Integer, Integer>();
        for (int i = 0; i < nums.length; ++i) {
            if (hashtable.containsKey(target - nums[i])) {
                return new int[]{hashtable.get(target - nums[i]), i};
            }
            hashtable.put(nums[i], i);
        }
        return new int[0];
    }
}
```



根据题目可知要找出和为目标值target的两个整数，首先我们想到的是使用**暴力枚举**。首先循环遍历数组，从中取出每一个元素x，然后再从x的后一位循环遍历数组，寻找x的后面是否有target-x。为什么要从i的后面寻找呢？因为你不能使用两次相同的元素，并且所有x之前的数已经和x匹配过了。这样的暴力枚举法虽然可以成功解题但是**效率不高**，因为它涉及到双层循环。时间复杂度为On的平方。

通过分析我们可以知道，暴力枚举的时间复杂度高，主要是因为它使用了双层循环，所以时间复杂度高主要是因为找target-x的时间复杂度高，得一个一个遍历着找，那么有没有什么方法可以提高找target-x的速度呢？不用一个一个的找？有的，因为我们学数据结构知道，**哈希表是增删改查平均性能都极佳的数据结构**。那我们就用哈希表，又因为我们需要返回两个数的索引，所以我们最好可以将数组中的元素和元素都存储在哈希表中，那我们想到的就是HashMap集合。

我们遍历数组中的数据，将没有匹配到它的另一半的数据先存进HashMap中，等着有人来匹配它。

**哈希表的增删改查效率为什么都很高？**因为，它会将你要查找的值输入hashCode()方法，计算出一个哈希值，是个整数。然后再根据这个整数和数组的长度计算出这个值在数组中的索引位置。然后直接去数组的index位置去找这个元素，进行增删改查操作就可以了。相当于**在不发生哈希冲突的情况下**，只需要进行一次计算和一次内存操作就可以完成增删改查等操作了，时间复杂度为O1。

**哈希冲突这里再单独在八股文中讲吧**。

**那上述的代码为什么效率是On呢，为什么比暴力枚举效率高呢**？**因为**：

- `hashtable.containsKey(key)`：平均时间复杂度为 **O(1)**。HashMap 通过计算键的哈希值直接定位到桶，理想情况下（无冲突或冲突很少）可以立即判断是否存在。
- `hashtable.get(key)`：平均时间复杂度为 **O(1)**，原理同上。
- `hashtable.put(key, value)`：平均时间复杂度为 **O(1)**。



```java
class Solution {
    public void moveZeroes(int[] nums) {
        int slow = 0 ;
        for(int i = 0; i < nums.length; i++ ){
            if(nums[i] != 0 ){
                nums[slow] = nums[i]; 
                slow ++ ; 
            }
        }
        for(int i = slow; i < nums.length; i++ ){
            nums[i] = 0 ; 
        }
    }
}
```



### 2、移动零

给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时保持非零元素的相对顺序。

**请注意** ，必须在不复制数组的情况下原地对数组进行操作。

**示例 1:**

```
输入: nums = [0,1,0,3,12]
输出: [1,3,12,0,0]
```



方法一：双指针法

```java
class Solution {
    public void moveZeroes(int[] nums) {
        int n = nums.length, left = 0, right = 0;
        while (right < n) {
            if (nums[right] != 0) {
                swap(nums, left, right);
                left++;
            }
            right++;
        }
    }

    public void swap(int[] nums, int left, int right) {
        int temp = nums[left];
        nums[left] = nums[right];
        nums[right] = temp;
    }
}
```



right:探索指针，从头到尾遍历数组，寻找非零元素。left:指向下一个非零元素应该被放置的位置。**操作**：每当right找到一个非零元素就与left指向的元素交换，然后两个指针都前进。优点：**实现了原地操作**，严格满足了“不复制数组的要求”，且一次循环就能完成。缺点：执行交换操作时，需要三次写入操作。

```java
class Solution {
    public void moveZeroes(int[] nums) {
        int slow = 0 ;
        for(int i = 0; i < nums.length; i++ ){
            if(nums[i] != 0 ){
                nums[slow] = nums[i]; 
                slow ++ ; 
            }
        }
        for(int i = slow; i < nums.length; i++ ){
            nums[i] = 0 ; 
        }
    }
}
```



方法二：也是使用了双指针技巧。

指针：i:快指针，功能同方法一的right。slow:慢指针，功能同方法一的left。**操作**：**第一遍循环**，只覆盖，快指针遇到非零数时，就将其赋值到慢指针slow的位置。然后slow前进。这是一个**单次写入**操作。**第二遍循环**：将数组尾部剩余的位置全部**填充为0**。优点：同方法一一样，**实现了原地操作，并且严格满足了“不复制数组的要求**。而且它的写入操作更少，每个非零元素只需要**一次写入**就能被移动到正确的位置。避免了三次写入的交换操作。缺点：虽然它有两次循环，但是，两次 O(n) 循环的时间复杂度仍然是 O(n)。所以其实没什么影响。

上述两个方法都采用了双指针的思想，实现了原地操作。直接在原来的数组上进行操作。并且进行的都是赋值操作、按索引查找数组元素操作、指针移动操作（说白了就++操作），这些操作的速度是很快的，**所以上面的两个方法的速度都是极快的**。**但是方法二的速度比方法一还要快。原因如下**：

1、**写入次数**这是最主要的因素。对于一个有k个元素的数组，**方法二需要执行k次写入操作**。方法一也需要执行几乎是k次交换操作，因为当right所指的元素不是0时就会执行交换操作。**一次交换操作是三次写入操作，所以方法一几乎要执行3k次写入操作**。因此方法二对内存的写入总量更少，**内存写入（尤其是跨越不同内存地址的写入）是相对昂贵的操作**，减少写入次数直接带来了性能提升。

2、**指令复杂度**：`赋值` 的指令比 `交换` 的指令更简单，CPU执行起来更快。

3、**缓存友好性**：方法二的连续赋值操作可能对CPU缓存更友好。它先集中处理非零数据，再集中填充0，访问模式更连续。而方法一的交换操作可能导致更随机的内存访问模式。

### 3、相交链表

方法一：哈希集合

判断两个链表是否相交，就是判断这两个链表中是否有地址值相同的节点。如果用暴力枚举法，那就是两层循环，第一层循环，遍历A链表，取出其中的每个节点，然后和B链表中的每个节点相比，看看是否是同一个节点。双层循环效率当然低的很喽！我们想要提高效率，我们的第一个想法就是A链表的循环遍历是不可避免的，只能从第二层循环下手去提高效率，我们知道，采取循环遍历去靠和一个一个元素去对比的方式查找元素是效率最低的一种方法。那我们学数据结构知道，**哈希表是增删改查效率都极佳的一个数据结构**，它的核心优势主要在于，它可以通过计算哈希值，然后再根据哈希值和桶数组的长度计算出该元素在桶数组中对应的位置，该位置下面挂着链表或者红黑树，这个元素就存储在其中，然后就快速的找到了该元素，相当于**在不发生哈希冲突的情况下**，只需要进行一次计算和一次内存操作就可以完成增删改查等操作了，时间复杂度为O1。所以我们**可以把第二层循环换成在哈希表中的查询操作**。

需要对比的是是否是同一个节点，需要返回的也是节点本身，所以我们就用HashSet集合存储A链表的每个节点就可以了。

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {
    public ListNode getIntersectionNode(ListNode headA, ListNode headB) {
        HashSet<ListNode> hsA = new HashSet<>();
        ListNode nextA = headA;
        while(nextA != null){
            hsA.add(nextA);
            nextA = nextA.next;
        }
        ListNode nextB = headB;
        while(nextB != null){
            boolean isNode = hsA.contains(nextB);
            if(isNode){
                return nextB;
            }
            nextB = nextB.next;
        }
        return null;
    }
}
```



**复杂度分析**：

时间复杂度：O(m+n)，其中 m 和 n 是分别是链表 headA 和 headB 的长度。需要遍历两个链表各一次。

空间复杂度：O(m)，其中 m 是链表 headA 的长度。需要使用哈希集合存储链表 headA 中的全部节点。

方法二：双指针

使用双指针的方法，可以将空间复杂度降至 *O*(1)。

创建两个指针 *pA* 和 *pB*，初始时分别指向两个链表的头节点 *headA* 和 *headB*，然后将两个指针依次遍历两个链表的每个节点。具体做法如下：

- 每步操作需要同时更新指针 pA 和 pB。

- 如果指针 pA 不为空，则将指针 pA 移到下一个节点；如果指针 pB 不为空，则将指针 pB 移到下一个节点。

- 如果指针 pA 为空，则将指针 pA 移到链表 headB 的头节点；如果指针 pB 为空，则将指针 pB 移到链表 headA 的头节点。

- 当指针 pA 和 pB 指向同一个节点或者都为空时，返回它们指向的节点或者 null。

**证明**：

下面提高双指针方法的正确性证明，考虑两种情况，第一种情况是两个链表相交，第二种情况是两个链表不相交。

情况一：两个链表相交

链表 *headA* 和 *headB* 的长度分别是x+z和y+z。假设链表 *headA* 的不相交部分有 x个节点，链表 *headB* 的不相交部分有 y 个节点，两个链表相交的部分有 z 个节点。

- 如果 *x*=*y*，则两个指针会同时到达两个链表相交的节点，此时返回相交的节点；

![image-20250908161151959](../Spring大礼包学习/img/image-20250908161151959.png)

- 如果 *a*\\=*b*，则指针 *pA* 会遍历完链表 *headA*，指针 *pB* 会遍历完链表 *headB*，两个指针不会同时到达链表的尾节点。如下图所示，链表headA的长度小于链表headB的长度。所以指针 *pA* 会先遍历完链表 *headA*，然后开始遍历链表headB。当指针pA再次指向两个链表的首个公共节点时，指针pA共走了x+z+y步。指针pB会先遍历完链表headB，然后再开始遍历链表headA，当指针pB再次指向两个链表的首个公共节点时，指针pB共走了y+z+x步。**两个指针同时开始走，每次操作都是同时更新指针pA和指针pB**，所以当这两个指针走了相同的步数

![image-20250908161242676](../Spring大礼包学习/img/image-20250908161242676.png)

5、

### 6、反转链表

### 7、回文链表

给你一个单链表的头节点 `head` ，请你判断该链表是否为回文链表。如果是，返回 `true` ；否则，返回 `false` 。

![image-20260103201725367](../Spring大礼包学习/img/image-20260103201725367.png)

方法一：（我一开始的写法）：

我一开始想到的就是双指针法，因为输入是一个单向链表，所以很难用双指针法。那么想到的就是将链表里的元素复制到底层是数组的容器中去，因为数组可以通过索引访问任何一个元素，时间复杂度为O(1)，所以想到的就是 ArrayList。然后使用双指针就OK啦。注意在将链表中的数据复制到 ArrayList 中时，不要改变了原来的链表，因为在实际开发中别人不希望链表的结构被改变。

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public boolean isPalindrome(ListNode head) {
        ArrayList<Integer> list = new ArrayList<>();
        
        ListNode currentNode = head;
        while (currentNode != null) {
            vals.add(currentNode.val);
            currentNode = currentNode.next;
        }
        int i = 0;
        int j = list.size() - 1;
        while (i < j) {
            if (!list.get(i).equals(list.get(j))) {
                return false;
            }
            i ++;
            j --;
        }
        return true;
    }
}
```

方法2：快慢指针

### 8、环形链表

**方法1：哈希表法：我一开始的方法：**

**思路**：题目中说“如果链表中有某个节点，可以通过连续跟踪 `next` 指针再次到达，则链表中存在环”。**那很容易想到的方法是，遍历这个链表，然后用一个容器装从这个链表中遍历出来的元素，每次遍历到一个节点时，判断该节点是否在之前被访问过**，怎么判断呢？要这个容器不就是想让它发挥这个作用吗？该节点在这个容器中就证明该节点之前访问过，不在就没访问过呗！那用哪个容器效率更高呢？HashMap呗！

思路和官方题解给出的方法一是一样的，区别就是我用的是 ArrayList 官方用的是 HashSet，我用的不对，因为 contains 是主要且频繁的操作，这个是根据元素本身去查找元素是否在容器里，当然是 HashSet 更快了，人家通过元素的哈希值和数组的长度一计算就知道是在桶数组的哪个位置，然后再在这个位置的链表或红黑树里面查找有没有这个元素，如果都没有发生哈希冲突的话，都没有链表或红黑树，直接通过元素的哈希值和数组长度一计算就知道有没有了，肯定是比 ArrayList 快，ArrayList 底层是基于 数组实现的，得挨个遍历才知道有没有。

用 HashSet 还有一点好处就是，利用它天然的去重机制，可以使代码更简洁。

所以官方题解用 HashSet 的方法：

```java
/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {
    public boolean hasCycle(ListNode head) {
         HashSet<ListNode> set = new HashSet<>();
         ListNode currently = head;
         
         while (currently != null) {
            if (!set.add(currently)){
                return true;
            }
            currently = currently.next;
         }
         return false;

    }
}
```



比我这个用 ArrayList 的方法要更好：

```java
/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {
    public boolean hasCycle(ListNode head) {
         ArrayList<ListNode> list = new ArrayList<>();
         ListNode currently = head;
         
         while (currently != null) {
            list.add(currently);
            currently = currently.next;
            if (currently != null && list.contains(currently)){
                return true;
            }
         }
         return false;

    }
}
```



**复杂度分析**：

- 时间复杂度：O(N)，其中 N 是链表中的节点数。最坏情况下我们需要遍历每个节点一次。

- 空间复杂度：O(N)，其中 N 是链表中的节点数。主要为哈希表的开销，最坏情况下我们需要将每个节点插入到哈希表中一次。

**方法2：快慢指针**

用到了**龟兔赛跑算法**，又称**Floyd 判圈算法** 的思想。

假想「乌龟」和「兔子」在链表上移动，「兔子」跑得快，「乌龟」跑得慢。当「乌龟」和「兔子」从链表上的同一个节点开始移动时，如果该链表中没有环，那么「兔子」将一直处于「乌龟」的前方；如果该链表中有环，那么「兔子」会先于「乌龟」进入环，并且一直在环内移动。等到「乌龟」进入环时，由于「兔子」的速度快，它一定会在某个时刻与乌龟相遇，**即套了「乌龟」若干圈**。

我们可以根据上述思路来解决本题。具体地，我们定义两个指针，一快一慢。慢指针每次只移动一步，而快指针每次移动两步。初始时，慢指针在位置 head，而快指针在位置 head.next。这样一来，如果在移动的过程中，快指针反过来追上慢指针，就说明该链表为环形链表。否则快指针将到达链表尾部，该链表不为环形链表。

为什么我们要规定初始时慢指针在位置 `head`，快指针在位置 `head.next`，而不是两个指针都在位置 `head`（即与「乌龟」和「兔子」中的叙述相同）？

- 观察下面的代码，我们使用的是 while 循环，循环条件先于循环体。由于循环条件一定是判断快慢指针是否重合，如果我们将两个指针初始都置于 head，那么 while 循环就不会执行。因此，我们可以假想一个在 head 之前的虚拟节点，慢指针从虚拟节点移动一步到达 head，快指针从虚拟节点移动两步到达 head.next，这样我们就可以使用 while 循环了。

- 当然，我们也可以使用 do-while 循环。此时，我们就可以把快慢指针的初始值都置为 head。

```java
public class Solution {
    public boolean hasCycle(ListNode head) {
        if (head == null || head.next == null) {
            return false;
        }
        ListNode slow = head;
        ListNode fast = head.next;
        while (slow != fast) {
            if (fast == null || fast.next == null) {
                return false;
            }
            slow = slow.next;
            fast = fast.next.next;
        }
        return true;
    }
}
```



上面的是官方题解，但其实还有一种更高明的写法，可以让快指针和慢指针都先指向 head，代码更简洁，且更省内存。

```java
/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class Solution {
    public boolean hasCycle(ListNode head) {
        ListNode slow = head;
        ListNode fast = head;
        while (fast != null && fast.next != null) {
            fast = fast.next.next;
            slow = slow.next;
            if (slow == fast) {
                return true;
            }
        }
        return false;
    }
}
```



**注意边界测试用例：**

总之就是涉及到链表的问题，你要注意，如果你的代码中用到了 .next 那你一定要确保，.next . 前面的的那个东西不为 null。

测试用例1：

head = []   pos = -1

测试用例2：
head = [1, 2] pos = -1

**复杂度分析**：

- 时间复杂度：*O*(*N*)，其中 *N* 是链表中的节点数。
  - 当链表中不存在环时，快指针将先于慢指针到达链表尾部，链表中每个节点至多被访问两次。
  - 当链表中存在环时，每一轮移动后，快慢指针的距离将减小一。而初始距离为环的长度，因此至多移动 *N* 轮。

- 空间复杂度：*O*(1)。我们只使用了两个指针的额外空间。

### 9、合并两个有序链表

**方法一：递归**

实在不想用递归

**方法二：迭代**

迭代法比递归法在这道题上更好，时间复杂度相似的情况下，空间复杂度为 O(1) 递归的空间复杂度为 O(n + m)。

其实我想到的就是这个方法：

当 list1 和 list 2 都不是空列表时，**因为是升序排序**，先判断`list1` 和 `list2` 哪一个链表的头节点的值更小，然后设定一个**哨兵节点 prehead**，让 prehead 指向那个较小的链表的头节点，这其实就相当于将较小值的节点添加到结果里，只不过是在原有的链表上进行操作，减少了空间的消耗。当一个节点被添加到结果里之后，将对应链表中的节点向后移一位。假设 list2 的头节点更小。

想到这里，你会更深一步的思考对不对？你会想：比啊比，比啊比，一直是 list1 的当前节点比 list2 的大，突然！比到了 list2 的当前节点是大节点的时候了，此时是不是就需要把 list1 的当前节点插到 形成的结果链的尾了？但是你能拿到当前形成的结果链的最后一个元素吗？拿不到吧！怎么办？所以就需要再设置一个 prev 节点，一直指向形成的结果链表的最后一元素，方便进行尾插。

然后，我们重复以下过程，直到 list1 或者 list2 指向了 null ：如果 list1 当前节点的值小于等于 list2 ，我们就把 list1 当前的节点接在 prev 节点的后面同时将 list1 指针往后移一位。否则，我们对 list2 做同样的操作。不管我们将哪一个元素接在了后面，我们都需要把 prev 向后移一位。

在循环终止的时候， **list1 和 list2 至多有一个是非空的**。由于输入的两个链表都是有序的，所以不管哪个链表是非空的，它包含的所有元素都比前面已经合并链表中的所有元素都要大。这意味着我们只需要简单地将非空链表接在合并链表的后面，并返回合并链表即可。

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode prehead = new ListNode(-1);
        ListNode prev = prehead;
        while (list1 != null && list2 != null) {
            if (list1.val > list2.val) {
                prev.next = list2;
                list2 = list2.next;
                
            }else {
                prev.next = list1;
                list1 = list1.next;
            }
            prev = prev.next;
        }
        prev.next = list1 == null ? list2 : list1;
        return prehead.next;
    }
}
```



**复杂度分析**：

- 时间复杂度：O(n+m)，其中 n 和 m 分别为两个链表的长度。因为每次循环迭代中，l1 和 l2 只有一个元素会被放进合并链表中， 因此 while 循环的次数不会超过两个链表的长度之和。所有其他操作的时间复杂度都是常数级别的，因此总的时间复杂度为 O(n+m)。


- 空间复杂度：*O*(1)。我们只需要常数的空间存放若干变量。

### 10、买卖股票的最佳时机

**方法一：暴力法**（不用）（会超时）

买卖股票么，题目说了，卖必须在买的**未来的某一个不同的日子**，其要求得是怎么买卖能获得最大利润，所以也不能拿买入时的价格减去卖出时的价格，这算成怎么买赔的最多了。

方法一用暴力法，用两层嵌套循环，我们需要找出给定数组中两个数字之间的最大差值（即，最大利润）。此外，第二个数字（卖出价格）必须大于第一个数字（买入价格）且卖必须在买的**未来的某一个不同的日子**。

形式上，对于每组 *i* 和 *j*（其中 *j*>*i*）我们需要找出 max(prices[j]−p**r**ices[i])。

```java
public class Solution {
    public int maxProfit(int[] prices) {
        int maxprofit = 0;
        for (int i = 0; i < prices.length - 1; i++) {
            for (int j = i + 1; j < prices.length; j++) {
                int profit = prices[j] - prices[i];
                if (profit > maxprofit) {
                    maxprofit = profit;
                }
            }
        }
        return maxprofit;
    }
}
```



**方法2：一次遍历**（力扣把这道题归类到了贪心算法、有人也把它归到了动态规划，啥是贪心算法啥是动态规划我还没弄明白）

我们来假设自己来购买股票。随着时间的推移，每天我们都可以选择出售股票与否。那么，假设在第 i 天，如果我们要在今天卖股票，那么我们能赚多少钱呢？

显然，如果我们真的在买卖股票，我们肯定会想：如果我是在历史最低点买的股票就好了！太好了，在题目中，我们只要用一个变量记录一个历史最低价格 minprice，我们就可以假设自己的股票是在那天买的。那么我们在第 i 天卖出股票能得到的利润就是 prices[i] - minprice。

因此，我们只需要遍历价格数组一遍，记录历史最低点，然后在每一天考虑这么一个问题：如果我是在历史最低点买进的，那么我今天卖出能赚多少钱？当考虑完所有天数之时，我们就得到了最好的答案。

```java
class Solution {
    public int maxProfit(int[] prices) {
        int minPrice = Integer.MAX_VALUE;
        int maxprofit = 0;
        for (int i = 0; i < prices.length; i ++) {
            if (prices[i] < minPrice) {
                minPrice = prices[i];
            }else if (prices[i] - minPrice > maxprofit) {
                maxprofit = prices[i] - minPrice;
            }
        }
        return maxprofit;
    }
}
```



### 11、爬楼梯

我们用 *f*(*x*) 表示爬到第 *x* 级台阶的方案数，考虑最后一步可能跨了一级台阶，也可能跨了两级台阶，所以我们可以列出如下式子：

*f*(*x*)=*f*(*x*−1)+*f*(*x*−2)

它意味着爬到第 *x* 级台阶的方案数是爬到第 *x*−1 级台阶的方案数和爬到第 *x*−2 级台阶的方案数的和。很好理解，因为每次只能爬 1 级或 2 级，比如，要爬第20级台阶了，那么**还差最后一步**到第20级台阶，只有两种方案，一种是当前在第19级台阶，再上一级台阶到第20级，另一种是当前在第18级台阶，直接跨两级台阶到第20级台阶。所以爬到第20级台阶的爬法就是爬到第18级台阶的爬法 + 爬到第19级台阶的爬法。以上是**动态规划的转移方程**，下面我们来**讨论边界条件**。我们是从第 0 级开始爬的，所以从第 0 级爬到第 0 级我们可以看作只有一种方案，即 *f*(0)=1；从第 0 级到第 1 级也只有一种方案，即爬一级，*f*(1)=1。这两个作为边界条件就可以继续向后推导出第 *n* 级的正确结果。我们不妨写几项来验证一下，**根据转移方程得到** *f*(2)=2，*f*(3)=3，*f*(4)=5，……，我们把这些情况都枚举出来，发现计算的结果是正确的。我们不难通过转移方程和边界条件给出一个时间复杂度和空间复杂度都是 *O*(*n*) 的实现，但是由于这里的 *f*(*x*) 只和 *f*(*x*−1) 与 *f*(*x*−2) 有关，所以我们可以用「**滚动数组思想**」把空间复杂度优化成 *O*(1)。**下面的代码中给出的就是这种实现。**

### 12、打家劫舍

好，截止目前为止，你看了B站上那个动态规划算法的视频，已经基本学会了动态规划了，你现在已经靠着自己，**做出了打家劫舍这道算法题了**，很棒！

**说一下我理解的动态规划算法的思想**，就是说一个问题，我们可以简单粗暴的将所有答案穷举出来，并画出递归树，尝试写一个递归函数来求解，如果发现遍历中存在大量的重复计算，我们可以尝试把数据缓存下来，之后遍历到相同的节点就直接查表，避免重复的计算，最后我们可以将计算的过程表示出来，然后观察计算的顺序，将递归形式改写成更加简洁高效的迭代形式。

再说一下，我做打家劫舍这道算法题时的思路：要偷到最高的金额，并且不能偷两家相邻的房屋，那么按照动态规划算法的思想，如果我从1号屋开始偷，那么相邻的2号屋肯定就不能去了，那第二个要去偷的可以是3号屋、4号屋、5号屋......。所以说，从1号屋开始偷，能偷到的最高金额就是选 {从3号屋开始偷能偷到的最高金额、从4号屋开始偷能偷到的最高金额、从5号屋开始偷能偷到的最高金额、...} 中的最大值，再加上1号屋的金额。以此类推，如果从2号屋开始偷，那么1、3号屋肯定是偷不了了，
